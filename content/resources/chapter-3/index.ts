import * as SoloResources from './solo'
import * as PoolResources from './pool'
import * as CoopResources from './coop'
import * as SplitResources from './split'

const Chapter3 = {
  solo: SoloResources,
  pool: PoolResources,
  coop: CoopResources,
  split: SplitResources,
}

export default Chapter3
